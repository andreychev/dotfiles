# Include shared base packages
instance_eval(File.read(File.expand_path("~/Brewfile.base")))

# Work-specific tools — add as needed

cask "bartender"

# AI
brew "can1357/tap/omp"
cask "chatgpt"

mas "Xcode", id: 497799835
